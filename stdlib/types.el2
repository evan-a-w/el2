type option(a) :=
  | Some(a)
  | None

let is_some(x) := match x with | &(None) -> false | _ -> true

let map_unit(x : option(a)) := {
  x?;
  Some(())
}

type result(ok, err) :=
  | Ok(ok)
  | Err(err)