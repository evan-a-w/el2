[* inner module *]

module Cool := {
  let f(x) := x + 1
}

let fib(x) :=
  match x == 0 || x == 1 with
  | true -> 1
  | false -> fib(x - 1) + fib(x - 2)

[* this gets evaluated at compile time because it can *]
let fib10 := fib(10)

let main() := fib10